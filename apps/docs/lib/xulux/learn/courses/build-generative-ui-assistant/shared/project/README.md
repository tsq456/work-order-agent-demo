# Build a Generative UI Assistant

This project is one canonical stage from the assistant-ui Learn course.

```bash
cp .env.example .env.local
npm install
npm run dev
```

Later stages use the OpenAI API key from `.env.local`. Each stage is a complete,
standalone snapshot that can be inspected, run, and downloaded.

