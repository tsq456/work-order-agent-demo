---
title: "Tooltip"
description: "A short label that appears on hover and stays out of the way."
links:
  - label: Source
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/react/ui/base/tooltip.tsx
  - label: Base UI Tooltip
    url: https://base-ui.com/react/components/tooltip
---

import { PreviewCode } from "@/components/pages/docs/preview-code.server";
import { KitInstall } from "@/components/pages/design/kit-install";
import {
  TooltipSpecimen,
  TooltipIconSpecimen,
} from "@/components/pages/design/specimens";
import * as RadixSpecimens from "@/components/pages/design/specimens.radix";
import { FlavorSwitcher } from "@/components/pages/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/pages/design/specimens" name="TooltipSpecimen" base={<TooltipSpecimen />}>
  <RadixSpecimens.TooltipSpecimen />
</PreviewCode>

## Installation

<KitInstall name="tooltip" />

## Usage

```tsx
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip";
import { Button } from "@/components/ui/button";

export function Example() {
  return (
    <Tooltip>
      <TooltipTrigger render={<Button variant="outline">Hover me</Button>} />
      <TooltipContent>Copy message</TooltipContent>
    </Tooltip>
  );
}
```

## Examples

### Icon buttons

A tooltip earns its place when the control is an icon alone; written labels do not need one.

<PreviewCode file="components/pages/design/specimens" name="TooltipIconSpecimen" base={<TooltipIconSpecimen />}>
  <RadixSpecimens.TooltipIconSpecimen />
</PreviewCode>

## API Reference

The parts wrap Base UI's Tooltip primitives and accept their props.

### TooltipContent

<ParametersTable
  type="TooltipContentProps"
  parameters={[
    {
      name: "side",
      type: '"top" | "right" | "bottom" | "left"',
      default: '"top"',
      description: "Preferred side of the trigger.",
    },
    {
      name: "sideOffset",
      type: "number",
      default: "4",
      description: "Distance from the trigger in pixels.",
    },
  ]}
/>

### TooltipProvider

Wrap a region to share hover delay across its tooltips.

<ParametersTable
  type="TooltipProviderProps"
  parameters={[
    {
      name: "delay",
      type: "number",
      description: "Milliseconds before the tooltip opens.",
    },
  ]}
/>
