---
title: "Command Tabs"
description: "One command in several dialects, with synced tabs and a copy button."
links:
  - label: Source
    url: https://github.com/assistant-ui/assistant-ui/blob/main/packages/ui/src/components/react/ui/base/command-tabs.tsx
---

import { PreviewCode } from "@/components/pages/docs/preview-code.server";
import { KitInstall } from "@/components/pages/design/kit-install";
import {
  CommandTabsSpecimen,
  CommandTabsSyncSpecimen,
} from "@/components/pages/design/specimens";
import * as RadixSpecimens from "@/components/pages/design/specimens.radix";
import { FlavorSwitcher } from "@/components/pages/docs/contexts/flavor.server";

<FlavorSwitcher />

<PreviewCode file="components/pages/design/specimens" name="CommandTabsSpecimen" base={<CommandTabsSpecimen />}>
  <RadixSpecimens.CommandTabsSpecimen />
</PreviewCode>

## Installation

<KitInstall name="command-tabs" />

## Usage

```tsx
import { CommandTabs } from "@/components/ui/command-tabs";

export function Example() {
  return (
    <CommandTabs
      storageKey="package-manager"
      commands={{
        npm: "npm install @assistant-ui/react",
        pnpm: "pnpm add @assistant-ui/react",
      }}
    />
  );
}
```

The command renders as Shiki-highlighted Bash; the copy button always copies the active dialect.

## Examples

### Synced instances

Two blocks sharing a `storageKey` switch together, and the choice survives reloads. Every install command on this site shares one key.

<PreviewCode file="components/pages/design/specimens" name="CommandTabsSyncSpecimen" base={<CommandTabsSyncSpecimen />}>
  <RadixSpecimens.CommandTabsSyncSpecimen />
</PreviewCode>

## API Reference

### CommandTabs

<ParametersTable
  type="CommandTabsProps"
  parameters={[
    {
      name: "commands",
      type: "Record<string, string>",
      required: true,
      description: "Tab label to command line, in display order.",
    },
    {
      name: "storageKey",
      type: "string",
      description: "Persists the selected tab in localStorage and keeps every instance with the same key in sync.",
    },
    {
      name: "onValueChange",
      type: "(value: string) => void",
      description: "Called when the user picks a tab.",
    },
    {
      name: "className",
      type: "string",
      description: "Additional CSS classes on the outer figure.",
    },
  ]}
/>
