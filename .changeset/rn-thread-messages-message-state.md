---
"@assistant-ui/react-native": patch
---

fix: type the `ThreadPrimitive.Messages` children `message` as the `MessageState` the package exports, which is the store state the primitive passes at runtime
