[project]
name = "state-test"
version = "0.1.0"
description = ""
authors = [
    {name = "Simon Farshid",email = "simon.farshid@outlook.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "assistant-stream",
    "fastapi>=0.115",
    "uvicorn>=0.34",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false

[tool.uv.sources]
assistant-stream = { path = "../assistant-stream", editable = true }
